/**
 * Arabic translations — tenant dashboard pages.
 *
 * Mirrors `en.dashboard.ts` key-for-key.
 */

import type { TranslationDictionary } from "../i18n.types";

const arDashboard: TranslationDictionary = {};

export default arDashboard;
